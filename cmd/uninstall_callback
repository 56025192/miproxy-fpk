#!/bin/bash

# $1: extension id

# Clean up miproxy files
ARCH=$(uname -m)
if [ "$ARCH" = "x86_64" ]; then
    ARCH_NAME="x86_64"
elif [ "$ARCH" = "aarch64" ]; then
    ARCH_NAME="arm64"
else
    ARCH_NAME="$ARCH"
fi

rm -f app/server/${ARCH_NAME}/miproxy
rm -f app/data/config.yaml
